---
title: "Snelstart"
description: "Maak je eerste API call in 5 minuten"
---

## Stap 1: API-sleutel ophalen

1. Log in op je [Flixer Pro dashboard](https://app.flixerpro.io)
2. Ga naar **Instellingen > API-sleutels**
3. Klik op **Nieuwe sleutel aanmaken**
4. Geef de sleutel een naam (bijv. "Website integratie")
5. Selecteer de benodigde scopes:
   - `leads:read` - Om leads op te halen
   - `leads:write` - Om leads aan te maken
6. Kopieer de gegenereerde sleutel (deze wordt maar één keer getoond!)

<Warning>
  Bewaar je API-sleutel veilig. Deel deze nooit in publieke code of repositories.
</Warning>

## Stap 2: Eerste request maken

Test je API-sleutel door de lijst met leads op te halen:

<CodeGroup>
```bash cURL
curl -X GET "https://api.flixerpro.io/v1/leads" \
  -H "X-API-Key: jouw_api_sleutel"
```

```javascript JavaScript
const response = await fetch("https://api.flixerpro.io/v1/leads", {
  headers: {
    "X-API-Key": "jouw_api_sleutel"
  }
});
const data = await response.json();
console.log(data);
```

```python Python
import requests

response = requests.get(
    "https://api.flixerpro.io/v1/leads",
    headers={"X-API-Key": "jouw_api_sleutel"}
)
print(response.json())
```
</CodeGroup>

Je ontvangt een JSON response met je leads:

```json
{
  "data": [
    {
      "id": "WRK...",
      "title": "Lekkage badkamer",
      "status": "request",
      "customer": {
        "name": "Jan de Vries"
      }
    }
  ],
  "limit": 50
}
```

## Stap 3: Lead aanmaken

Maak je eerste lead aan:

<CodeGroup>
```bash cURL
curl -X POST "https://api.flixerpro.io/v1/leads" \
  -H "X-API-Key: jouw_api_sleutel" \
  -H "Content-Type: application/json" \
  -d '{
    "street": "Keizersgracht",
    "streetNumber": "123",
    "postalCode": "1015 CJ",
    "city": "Amsterdam",
    "customerName": "Jan de Vries",
    "email": "jan@voorbeeld.nl",
    "phone": "+31612345678",
    "title": "Lekkage badkamer",
    "description": "Waterdruppels op het plafond"
  }'
```

```javascript JavaScript
const response = await fetch("https://api.flixerpro.io/v1/leads", {
  method: "POST",
  headers: {
    "X-API-Key": "jouw_api_sleutel",
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    street: "Keizersgracht",
    streetNumber: "123",
    postalCode: "1015 CJ",
    city: "Amsterdam",
    customerName: "Jan de Vries",
    email: "jan@voorbeeld.nl",
    phone: "+31612345678",
    title: "Lekkage badkamer",
    description: "Waterdruppels op het plafond"
  })
});
const lead = await response.json();
console.log("Lead aangemaakt:", lead.id);
```

```python Python
import requests

response = requests.post(
    "https://api.flixerpro.io/v1/leads",
    headers={
        "X-API-Key": "jouw_api_sleutel",
        "Content-Type": "application/json"
    },
    json={
        "street": "Keizersgracht",
        "streetNumber": "123",
        "postalCode": "1015 CJ",
        "city": "Amsterdam",
        "customerName": "Jan de Vries",
        "email": "jan@voorbeeld.nl",
        "phone": "+31612345678",
        "title": "Lekkage badkamer",
        "description": "Waterdruppels op het plafond"
    }
)
print("Lead aangemaakt:", response.json()["id"])
```
</CodeGroup>

<Check>
  Gefeliciteerd! Je hebt je eerste lead aangemaakt via de API.
</Check>

## Volgende stappen

<CardGroup cols={2}>
  <Card title="Authenticatie" icon="key" href="/authentication">
    Leer meer over API-sleutels en scopes
  </Card>
  <Card title="Lead capture" icon="file-import" href="/guides/lead-capture">
    Integreer lead capture in je website
  </Card>
</CardGroup>
